#version 450

layout(local_size_x=512, local_size_y=1, local_size_z=1) in;

layout (set=0,binding = 0) buffer StorageBuffer{
    float data[];
} storageBuffer;

void main(){
    uint index = gl_GlobalInvocationID.x;
    storageBuffer.data[index] += 1.0f;
}

